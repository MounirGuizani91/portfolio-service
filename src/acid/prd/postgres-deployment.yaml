apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-prd
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-prd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-prd
  template:
    metadata:
      labels:
        app: postgres-prd
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_DB
              value: portfolio
            - name: POSTGRES_USER
              value: admin
            - name: POSTGRES_PASSWORD
              value: admin
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: postgres-pvc-prd
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service-prd
spec:
  type: ClusterIP
  selector:
    app: postgres-prd
  ports:
    - port: 5432
      targetPort: 5432